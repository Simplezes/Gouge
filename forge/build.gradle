plugins {
    id 'java'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
    id 'org.spongepowered.mixin' version '0.7-SNAPSHOT'
}

version = mod_version
group = maven_group
base.archivesName = archives_base_name

java.toolchain.languageVersion = JavaLanguageVersion.of(21)

sourceSets.main.output.resourcesDir = sourceSets.main.output.classesDirs.singleFile

sourceSets.main.java.srcDir file("../common/src/main/java")
sourceSets.main.resources.srcDir file("../common/src/main/resources")

minecraft {
    mappings channel: 'official', version: minecraft_version
    runs {
        client {
            workingDirectory project.file('run')
            mods { gouge { source sourceSets.main } }
        }
        server {
            workingDirectory project.file('run')
            mods { gouge { source sourceSets.main } }
        }
    }
}

mixin {
    add sourceSets.main, 'gouge.refmap.json'
    config 'gouge.mixins.json'
}

repositories {
    mavenCentral()
}

dependencies {
    minecraft "net.minecraftforge:forge:${minecraft_version}-${forge_version}"
    annotationProcessor 'org.spongepowered:mixin:0.8.5:processor'
    implementation "com.electronwill.night-config:toml:3.8.1"
    jarJar(group: 'com.electronwill.night-config', name: 'toml', version: '[3.8,3.9)') {
        jarJar.pin(it, '3.8.1')
    }
    jarJar(group: 'com.electronwill.night-config', name: 'core', version: '[3.8,3.9)') {
        jarJar.pin(it, '3.8.1')
    }
}

jar {
    manifest {
        attributes([
            'MixinConfigs': 'gouge.mixins.json',
            'Implementation-Version': project.version
        ])
    }
}

build.dependsOn(tasks.named('jarJar'))
